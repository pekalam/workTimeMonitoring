<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" x:Class="WindowUI.StartWork.StartWorkView"             
             prism:ViewModelLocator.AutoWireViewModel="True" d:DesignWidth="1200" d:DesignHeight="800">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Padding="10,15,10,0">

        
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MaxWidth="500"/>
            </Grid.ColumnDefinitions>
                <TextBlock  Text="Monitoring start" Margin="0,10,0,10"/>
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2.5*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <mah:TimePicker Culture="{Binding CurrentCulture, Mode=OneWay}"
                                    HandVisibility="{Binding Path=SelectedItem, ElementName=DateTimePickerHandVisibility, Mode=TwoWay}"
                                    IsEnabled="{Binding Path=IsChecked, ElementName=AutoStartCheckBox}"
                                    PickerVisibility="HourMinute"
                                    SelectedDateTime="{Binding Path=SelectedDateTime, ElementName=DateTimePicker}"
                                    SelectedTimeFormat="{Binding Path=SelectedItem, ElementName=DateTimePickerTimeFormat}" />
                    <CheckBox Margin="18,0,0,0" Grid.Column="1" Name="AutoStartCheckBox"
                              Content="Start automatically" HorizontalAlignment="Right" VerticalAlignment="Center" />
                </Grid>
                <TextBlock Grid.Row="2" Text="Monitoring end"  Margin="0,10,0,10"/>
                <mah:TimePicker Grid.Row="3" Culture="{Binding CurrentCulture, Mode=OneWay}"
                                HandVisibility="{Binding Path=SelectedItem, ElementName=DateTimePickerHandVisibility, Mode=TwoWay}"
                                IsEnabled="True"
                                PickerVisibility="HourMinute"
                                SelectedDateTime="{Binding Path=SelectedDateTime, ElementName=DateTimePicker}"
                                SelectedTimeFormat="{Binding Path=SelectedItem, ElementName=DateTimePickerTimeFormat}" 
                                Margin="0,0,0,20"/>

                <Button Grid.Row="4" MaxWidth="200">Start work</Button>
            </Grid>
        </Border>
        <Border Grid.Column="1" Padding="10,15,10,0">
            <StackPanel >
                <TextBlock HorizontalAlignment="Left" Text="Started"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
