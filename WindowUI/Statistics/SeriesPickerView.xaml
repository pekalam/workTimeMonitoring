<UserControl x:Class="WindowUI.Statistics.SeriesPickerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WindowUI.Statistics"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="10"/>
        </Style>
    </UserControl.Resources>
    <StackPanel Background="White">
        <CheckBox Content="Mouse" HorizontalAlignment="Left" VerticalAlignment="Center"
                  IsChecked="{Binding ShowMouse}"/>
        <CheckBox Content="Watching screen" HorizontalAlignment="Left" VerticalAlignment="Center"
                  IsChecked="{Binding ShowWatchingScreen}"/>
        <CheckBox Content="Keyboard" HorizontalAlignment="Left" VerticalAlignment="Center"
                  IsChecked="{Binding ShowKeyboard}"/>
        <CheckBox Content="Away" HorizontalAlignment="Left" VerticalAlignment="Center"
                  IsChecked="{Binding ShowAway}"/>
    </StackPanel>

</UserControl>